基本的に
http://www.synaesthesia.jp/subversion/
(TortoiseSVN、Subversionをさくらインターネットで簡単に使う方法.pdf)
の手順の通り実行

その後コマンドパスを通す
%vi .cshrc
set path = (～) の () 内に
 $HOME/local/bin
を追加

puttygen.exe で秘密鍵を生成 ---
OpenSSL の公開鍵
openssh_pub_key.txt
を FTP などで自分のディレクトリ（/home/kishinet/）の .ssh ディレクトリにコピーする。
putty で kishinet.sakura.ne.jp にログインする。
% cd .ssh
% chmod 700 ./
% cat openssh_pub_key.txt >> authorized_keys
% chmod 600 authorized_keys
% rm openssh_pub_key.txt